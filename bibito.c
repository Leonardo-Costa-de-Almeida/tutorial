#include <stdio.h>
#include <stdlib.h>

int main() {
  int contador;
  contador = 0;
  for (contador = 100; contador >= 20; contador--) {
    printf("\n %i", contador);
  }
  // comentario1
  system("PAUSE");
}
